# Kartografie pro geology

Podsložka webu `nikolak97.github.io`. Sestavuje se kořenovým `_config.yml` (téma minima, `permalink: /:path/`), proto zde není vlastní konfigurace.

- Adresa: `https://nikolak97.github.io/geocartography/`
- Všechny interní odkazy mají prefix `/geocartography/`.
- Chcete-li kurz v horním menu, přidejte do kořenového `_config.yml` pod `header_pages` řádek `- geocartography/index.md`.

Struktura: `index.md`, `hodnoceni.md`, `data.md`, `arcgis-nastroje.md`, `literatura.md`, `lessons/` (10 lekcí), `assignments/` (3 úkoly + zápočet).
